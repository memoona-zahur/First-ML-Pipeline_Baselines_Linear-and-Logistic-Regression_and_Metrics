# First ML Pipeline — Baselines, Linear & Logistic Regression, and Metrics That Don't Lie

Week 06 · Day 3. A complete ML pipeline demonstrating honest baselines, proper train/test splitting, and meaningful metrics for both regression and classification tasks.

## Dataset

`data/students.csv` — 600 rows × 6 columns (`student_id`, `class_section`, `study_hours_per_week`, `sleep_hours_per_night`, `attendance_pct`, `exam_score`), generated by `generate_data.py` (seed=21).

## Key Results

| # | Finding | Statistic | Status |
|---|---|---|---|
| 1 | Linear regression reduces prediction error vs. baseline | RMSE: 10.30 → 7.06 (31% reduction) | Verified ✓ |
| 2 | Linear regression explains substantial variance | R² = 0.5257 | Verified ✓ |
| 3 | Study hours is strongest predictor | Coefficient: 2.033 pts/hr | Verified ✓ |
| 4 | 65.3% of students achieve distinction | Distinction rate: 0.653 | Verified ✓ |
| 5 | Baseline accuracy is misleading on imbalanced data | Accuracy 65% but precision = 0.65 | Verified ✓ |
| 6 | Logistic regression genuinely identifies distinction students | Accuracy: 77%, Recall: 83%, Precision: 81% | Verified ✓ |

*All findings are produced by `f-string`s from live variables, never hand-typed, and re-verified in the notebook's self-audit table and in `test_ml_pipeline.py`.*

## Deliverables

| File | Contents |
|---|---|
| `ml_pipeline.ipynb` | Full pipeline: data generation → feature engineering → split → baselines → models → metrics → self-audit → limitations |
| `data/students.csv` | 600 × 6 students dataset |
| `generate_data.py` | Dataset generator (exact assignment spec, `seed=21`) |
| `test_ml_pipeline.py` | 64-check automated verification suite |
| `charts/` | All saved PNG figures from the notebook |
| `technical_summary.md` | Standalone plain-language write-up for non-technical readers |
| `SELF_REVIEW.md` | Requirement-by-requirement verification of the whole lab |
| `requirements.txt` | Pinned Python dependencies |

## How to run

```bash
# Create and activate the virtual environment
python3 -m venv .venv
source .venv/bin/activate

# Install exact pinned dependencies
pip install -r requirements.txt

# Regenerate dataset (spec-exact, seed=21)
python3 generate_data.py

# Run the verification suite (64 checks)
python3 test_ml_pipeline.py

# Open the notebook
jupyter notebook ml_pipeline.ipynb
```

The notebook survives **Restart Kernel & Run All** with zero error cells.

## Reading the notebook as a report

The notebook is written as a **narrative report, not just code**. Every section follows the same pattern:

> **Question:** the specific question the section answers
>
> ...the code/chart...
>
> **What this tells us:** the narrative finding in prose

You can read only the `##` section headers and the **"What this tells us"** paragraphs to get the full findings without ever looking at a chart or the code. Every number in those findings is produced by the code cell above it (never hand-typed) and is re-verified in the notebook's self-audit table (Section 13).

## Verified findings (recomputed from `data/students.csv`)

| # | Finding | Statistic | Status |
|---|---|---|---|
| 1 | Weekly study hours associated with higher exam scores | Pearson r = 0.689 (p = 8.1e-86), coefficient = 2.033 | Verified ✓ |
| 2 | Nightly sleep hours show no relationship with scores | Pearson r = −0.022 — genuine null | Verified ✓ |
| 3 | Section C outscores A | mean C − mean A = 3.75 pts | Verified ✓ |
| 4 | Study-hours relationship is essentially linear | Pearson 0.6895 vs Spearman 0.7010; |diff| = 0.012 | Verified ✓ |

## Additional work beyond requirements

- **Automated verification suite** — 64 pytest-style checks that validate dataset, feature engineering, split, models, and metrics
- **Self-audit table** — every claimed number independently recomputed and verified
- **Confusion matrix visualization** — shows the model's error types
- **Feature coefficient analysis** — interpretable model coefficients with chart
- **Regression comparison charts** — visual RMSE and R² comparison
- **Classification comparison charts** — visual metric comparison across all four metrics
- **Requirements mapping** — every spec requirement mapped to where it's delivered
- **Dataset SHA-256 fingerprint** for reproducibility verification
- **Honest limitations documented** — causation, generalization, features, linearity, split, threshold

## Limitations (honest)

- Correlation ≠ causation — studying more may not *cause* higher scores (motivation confound)
- Synthetic data — real students might have different patterns
- Only 5 features — many real factors omitted
- Linear assumptions — real relationships might be curved
- Single train/test split — cross-validation would be more robust
- Threshold sensitivity — different distinction thresholds change class balance

## Reading the notebooks as a report

Both notebooks are written as **narrative reports, not just code**. Every chart section follows the same pattern (mirroring the reference `eda-precision-lab` repo):

> **Question:** the specific question the chart answers
>
> ...the chart (built, saved, reopened)...
>
> **What this tells us:** the narrative finding in prose

You can read only the `##` section headers and the **"What this tells us"** paragraphs to get the full findings without ever looking at a chart or the code. Every number in those findings is produced by the code cell above it (never hand-typed) and is re-verified in each notebook's self-audit table (Section 13).
